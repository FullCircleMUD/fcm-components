# SPDX-License-Identifier: BSD-3-Clause
"""FCM's shared components, one module per component.

Each component is a self-contained module with its own interface, its own
consumers and its own section of the test plan. The library is the container;
the module is the unit that gets refactored, promoted, or retired.

No components yet — see docs/INDEX.md for the design wiki.
"""

__version__ = "0.0.1"
